# Pre-commit hooks for kg-builder
# Install: pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer
      - id: check-yaml
        exclude: ^docker/
      - id: check-json
      - id: check-toml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: mixed-line-ending
        args: ['--fix=lf']
      - id: detect-private-key

  # Ruff - Python linter and formatter (replaces black, isort, flake8)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0
    hooks:
      # Run the linter
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      # Run the formatter
      - id: ruff-format

  # Mypy - Type checking
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.8.0
    hooks:
      - id: mypy
        args: [--ignore-missing-imports, --explicit-package-bases]
        additional_dependencies:
          - types-requests
          - types-PyYAML
        exclude: ^(tests/|examples/|scripts/)

  # Bandit - Security linting
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.6
    hooks:
      - id: bandit
        args: [-c, pyproject.toml]
        additional_dependencies: ["bandit[toml]"]
        exclude: ^tests/

  # Prettier - Format markdown, YAML, JSON
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.1.0
    hooks:
      - id: prettier
        types_or: [markdown, yaml, json]
        exclude: ^(data/|.*\.ipynb$)

  # Jupyter notebook cleaning (optional, uncomment if using notebooks)
  # - repo: https://github.com/nbQA-dev/nbQA
  #   rev: 1.7.1
  #   hooks:
  #     - id: nbqa-ruff
  #       args: [--fix]
  #     - id: nbqa-mypy
  #       args: [--ignore-missing-imports]

  # Check requirements.txt is up to date (optional)
  # - repo: local
  #   hooks:
  #     - id: check-requirements
  #       name: Check requirements.txt is compiled
  #       entry: bash -c 'uv pip compile requirements.in --quiet | diff -q - requirements.txt || (echo "requirements.txt is out of date. Run: uv pip compile requirements.in -o requirements.txt"; exit 1)'
  #       language: system
  #       files: ^requirements\.(in|txt)$
  #       pass_filenames: false
